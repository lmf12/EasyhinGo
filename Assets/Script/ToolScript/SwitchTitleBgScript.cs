using UnityEngine;
using System.Collections;

public class SwitchTitleBgScript : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
		transform.position = new Vector2 (0, Screen.height);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
